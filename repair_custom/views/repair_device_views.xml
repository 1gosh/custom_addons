<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

    <!-- stock.lot HiFi tree view extension (adds repair fields) -->
    <record id="view_hifi_unit_tree_inherit" model="ir.ui.view">
        <field name="name">stock.lot.hifi.tree.inherit.repair_custom</field>
        <field name="model">stock.lot</field>
        <field name="inherit_id" ref="repair_devices.view_hifi_unit_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='hifi_partner_id']" position="after">
                <field name="functional_state" widget="badge"
                    decoration-danger="functional_state == 'broken'"
                    decoration-info="functional_state == 'fixing'"
                    decoration-success="functional_state == 'working'"/>
                <field name="stock_state" widget="badge" optional="show"/>
                <field name="repair_order_count"/>
                <field name="warranty_state" column_invisible="1"/>
                <field name="warranty_type" optional="show" widget="badge"
                    decoration-success="warranty_state == 'active'"
                    decoration-muted="warranty_state != 'active'"/>
            </xpath>
        </field>
    </record>

    <!-- stock.lot HiFi form view extension (adds repair/warranty fields) -->
    <record id="view_hifi_unit_form_inherit" model="ir.ui.view">
        <field name="name">stock.lot.hifi.form.inherit.repair_custom</field>
        <field name="model">stock.lot</field>
        <field name="inherit_id" ref="repair_devices.view_hifi_unit_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='hifi_device_group']" position="before">
                <field name="functional_state" invisible="1"/>
                <field name="warranty_state" invisible="1"/>
                <field name="warranty_type" invisible="1"/>
                <field name="is_hifi_unit" invisible="1"/>
                <widget name="web_ribbon" title="En Panne" bg_color="bg-danger" invisible="functional_state != 'broken' or not is_hifi_unit"/>
                <widget name="web_ribbon" title="Fonctionnel" bg_color="bg-success" invisible="functional_state != 'working' or not is_hifi_unit"/>
                <div class="alert alert-success text-center" role="alert" invisible="warranty_state != 'active' or not is_hifi_unit">
                    <div class="d-flex align-items-center justify-content-center">
                        <i class="fa fa-shield me-1"/>
                        <strong>Garantie <field name="warranty_type" readonly="1" class="d-inline"/> active</strong> — Expire le <field name="warranty_expiry" readonly="1" class="d-inline"/>
                    </div>
                </div>
                <div class="alert alert-secondary text-center" role="alert" invisible="warranty_state != 'expired' or not is_hifi_unit">
                    <div class="d-flex align-items-center justify-content-center">
                        <i class="fa fa-shield me-1"/>
                        Garantie expirée (dernière expiration : <field name="warranty_expiry" readonly="1" class="d-inline"/>)
                    </div>
                </div>
            </xpath>
            <xpath expr="//group[@name='hifi_device_group']" position="after">
                <group invisible="not is_hifi_unit">
                    <group>
                        <field name="stock_state" widget="badge"/>
                    </group>
                </group>
                <notebook invisible="not is_hifi_unit">
                    <page string="Réparations associées" invisible="not repair_order_ids">
                        <field name="repair_order_ids" readonly="1">
                            <tree string="Repairs order" multi_edit="1" sample="1" decoration-info="state == 'draft'">
                                <field name="name" string="Référence" optional="show"/>
                                <field name="entry_date" optional="show" string="Date d'entrée" readonly="True"
                                    widget="datetime" options="{'show_time':false}" />
                                <field name="state" optional="show" widget="badge" decoration-success="state == 'done'"
                                    decoration-info="state == 'confirmed'" decoration-warning="state == 'under_repair'"
                                    decoration-danger="state == 'cancel'" decoration-muted="state == 'draft'" readonly="True"
                                    width="50" on_change="1" />
                                <field name="repair_warranty" optional="show"/>
                                <field name="partner_id" readonly="1" optional="show" string="Client" width="100" on_change="1" />
                                <field name="technician_employee_id" optional="show"/>
                            </tree>
                        </field>
                    </page>
                    <page string="Garantie" invisible="warranty_state == 'none'">
                        <group>
                            <group>
                                <field name="warranty_type" widget="badge"
                                    decoration-success="warranty_state == 'active'"
                                    decoration-muted="warranty_state != 'active'"/>
                                <field name="warranty_expiry"/>
                                <field name="warranty_state" widget="badge"
                                    decoration-success="warranty_state == 'active'"
                                    decoration-danger="warranty_state == 'expired'"/>
                            </group>
                            <group>
                                <field name="sale_date" invisible="not sale_date"/>
                                <field name="sav_expiry" invisible="not sav_expiry"/>
                                <field name="sale_order_id" invisible="not sale_order_id"/>
                                <field name="last_delivered_repair_id" invisible="not last_delivered_repair_id"/>
                                <field name="sar_expiry" invisible="not sar_expiry"/>
                            </group>
                        </group>
                    </page>
                </notebook>
            </xpath>
        </field>
    </record>

    <!-- stock.lot HiFi search view extension (adds repair filters) -->
    <record id="view_hifi_unit_search_inherit" model="ir.ui.view">
        <field name="name">stock.lot.hifi.search.inherit.repair_custom</field>
        <field name="model">stock.lot</field>
        <field name="inherit_id" ref="repair_devices.view_hifi_unit_search"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='hifi_variant_id']" position="after">
                <field name="stock_state"/>
                <filter name="filter_stock_client" string="Propriété Client" domain="[('stock_state', '=', 'client')]"/>
                <filter name="filter_stock_stock" string="En Stock" domain="[('stock_state', '=', 'stock')]"/>
                <filter name="filter_stock_rented" string="En Location" domain="[('stock_state', '=', 'rented')]"/>
                <separator/>
            </xpath>
            <xpath expr="//group[@name='group_by']" position="inside">
                <filter name="group_stock_state" string="Statut Stock" context="{'group_by': 'stock_state'}"/>
            </xpath>
        </field>
    </record>

    </data>
</odoo>
