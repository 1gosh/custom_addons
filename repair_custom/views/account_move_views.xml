<odoo>
    <record id="view_move_form_inherit_repair" model="ir.ui.view">
        <field name="name">account.move.form.inherit.repair</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_open_repair"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-wrench"
                        invisible="not repair_id or batch_id">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Réparation</span>
                        <field name="repair_id" nolabel="1" readonly="1" class="o_stat_value"/>
                    </div>
                </button>
                <button name="action_open_batch"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-folder-open-o"
                        invisible="not batch_id">
                    <div class="o_stat_info">
                        <span class="o_stat_text">Dossier de réparation</span>
                        <field name="batch_id" nolabel="1" readonly="1" class="o_stat_value"/>
                    </div>
                </button>
            </xpath>

            <xpath expr="//field[@name='partner_id']" position="after">
                <field name="batch_id" invisible="1"/>
                <field name="repair_id" invisible="1"/>
            </xpath>

        </field>
    </record>
</odoo>
